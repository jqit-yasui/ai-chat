# OpenRouter API キー（https://openrouter.ai/keys で発行）
OPENROUTER_API_KEY=

# MongoDB Atlas 接続文字列（Prisma 用）
DATABASE_URL=

# 本番環境（Cloud Run）向け Basic 認証（middleware.ts が参照。NODE_ENV=production 時のみ適用される）
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
